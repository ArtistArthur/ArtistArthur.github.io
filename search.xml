<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Josephus problem(约瑟夫问题)</title>
      <link href="/2020/06/26/josephus-problem/"/>
      <url>/2020/06/26/josephus-problem/</url>
      
        <content type="html"><![CDATA[<h1 id="Josephus-problem（约瑟夫问题）"><a href="#Josephus-problem（约瑟夫问题）" class="headerlink" title="Josephus problem（约瑟夫问题）"></a>Josephus problem（约瑟夫问题）</h1><h2 id="问题"><a href="#问题" class="headerlink" title="问题"></a>问题</h2><blockquote><p>约瑟夫是犹太军队的一个将军，在反抗罗马的起义中，他所率领的军队被击溃，只剩下残余的部队40余人，他们都是宁死不屈的人，所以不愿投降做叛徒。一群人表决说要死，所以用一种策略来先后杀死所有人。<br>于是约瑟夫建议：每次由其他两人一起杀死一个人，而被杀的人的先后顺序是由抽签决定的，约瑟夫有预谋地抽到了最后一签，在杀了除了他和剩余那个人之外的最后一人，他劝服了另外一个没死的人投降了罗马。<br>规则：<br>在一间房间总共有n个人（下标0～n-1），只能有最后一个人活命。<br>按照如下规则去杀人：<br>所有人围成一圈<br>顺时针报数，每次报到q的人将被杀掉<br>被杀掉的人将从房间内被移走<br>然后从被杀掉的下一个人重新报数，直到剩余一人</p></blockquote><p>那么如何选择一个位置以使得你变成那剩余的最后一人？</p><p>这个问题可以转换成递推问题：$J(n,k)=(J(n-1,k)+k)\%n$</p><ul><li>$J(n,k)$表示人数为n，报数节点为k，最后剩下的人的编号。</li><li>对于$J(n,k)$,可以转换成 $J(n-1,k)$</li><li>当人数为$n$时，进行第一次报数，第$k$个人退出，此时剩余$n-1$个人。</li><li>若重新排号，第$k+1$个人为1号，那么此时问题为 $J(n-1,k)$,而此时求出的结果的编号是人数为$n-1$并且重新排号的情况，$J(n,k)$与$J(n-1,k)$编号的关系为 $J(n,k)=(J(n-1,k)+k)\%n$ (自行体会)。</li></ul><p>//注意事项：重新编号应该从0开始，最后要和题目的编号方式比较再输出。</p><p><a href="http://bailian.openjudge.cn/practice/2746/" target="_blank" rel="noopener">百练题目地址</a></p><script>        document.querySelectorAll('.github-emoji')          .forEach(el => {            if (!el.dataset.src) { return; }            const img = document.createElement('img');            img.style = 'display:none !important;';            img.src = el.dataset.src;            img.addEventListener('error', () => {              img.remove();              el.style.color = 'inherit';              el.style.backgroundImage = 'none';              el.style.background = 'none';            });            img.addEventListener('load', () => {              img.remove();            });            document.body.appendChild(img);          });      </script>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>post</title>
      <link href="/2020/06/26/post-1/"/>
      <url>/2020/06/26/post-1/</url>
      
        <content type="html"><![CDATA[<script>        document.querySelectorAll('.github-emoji')          .forEach(el => {            if (!el.dataset.src) { return; }            const img = document.createElement('img');            img.style = 'display:none !important;';            img.src = el.dataset.src;            img.addEventListener('error', () => {              img.remove();              el.style.color = 'inherit';              el.style.backgroundImage = 'none';              el.style.background = 'none';            });            img.addEventListener('load', () => {              img.remove();            });            document.body.appendChild(img);          });      </script>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>post</title>
      <link href="/2020/06/26/post/"/>
      <url>/2020/06/26/post/</url>
      
        <content type="html"><![CDATA[<script>        document.querySelectorAll('.github-emoji')          .forEach(el => {            if (!el.dataset.src) { return; }            const img = document.createElement('img');            img.style = 'display:none !important;';            img.src = el.dataset.src;            img.addEventListener('error', () => {              img.remove();              el.style.color = 'inherit';              el.style.backgroundImage = 'none';              el.style.background = 'none';            });            img.addEventListener('load', () => {              img.remove();            });            document.body.appendChild(img);          });      </script>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>typora-vue-theme主题介绍</title>
      <link href="/2020/06/25/create-hexo/"/>
      <url>/2020/06/25/create-hexo/</url>
      
        <content type="html"><![CDATA[<h1 id="从零开始的-JSON-库教程（一）：启程"><a href="#从零开始的-JSON-库教程（一）：启程" class="headerlink" title="从零开始的 JSON 库教程（一）：启程"></a>从零开始的 JSON 库教程（一）：启程</h1><ul><li>Milo Yip</li><li>2016/9/15</li></ul><p>本文是<a href="https://zhuanlan.zhihu.com/json-tutorial" target="_blank" rel="noopener">《从零开始的 JSON 库教程》</a>的第一个单元。教程练习源代码位于 <a href="https://github.com/miloyip/json-tutorial" target="_blank" rel="noopener">json-tutorial</a>。</p><p>本单元内容：</p><ul><li><a href="#从零开始的-json-库教程一启程">从零开始的 JSON 库教程（一）：启程</a><ul><li><a href="#1-json-是什么">1. JSON 是什么</a></li><li><a href="#2-搭建编译环境">2. 搭建编译环境</a></li><li><a href="#3-头文件与-api-设计">3. 头文件与 API 设计</a></li><li><a href="#4-json-语法子集">4. JSON 语法子集</a></li><li><a href="#5-单元测试">5. 单元测试</a></li><li><a href="#6-宏的编写技巧">6. 宏的编写技巧</a></li><li><a href="#7-实现解析器">7. 实现解析器</a></li><li><a href="#8-关于断言">8. 关于断言</a></li><li><a href="#9-总结与练习">9. 总结与练习</a></li><li><a href="#10-常见问答">10. 常见问答</a></li></ul></li></ul><h2 id="1-JSON-是什么"><a href="#1-JSON-是什么" class="headerlink" title="1. JSON 是什么"></a>1. JSON 是什么</h2><p>JSON（JavaScript Object Notation）是一个用于数据交换的文本格式，现时的标准为<a href="https://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf" target="_blank" rel="noopener">ECMA-404</a>。</p><p>虽然 JSON 源至于 JavaScript 语言，但它只是一种数据格式，可用于任何编程语言。现时具类似功能的格式有 XML、YAML，当中以 JSON 的语法最为简单。</p><p>例如，一个动态网页想从服务器获得数据时，服务器从数据库查找数据，然后把数据转换成 JSON 文本格式：</p><pre class="line-numbers language-lang-js"><code class="language-lang-js">{    "title": "Design Patterns",    "subtitle": "Elements of Reusable Object-Oriented Software",    "author": [        "Erich Gamma",        "Richard Helm",        "Ralph Johnson",        "John Vlissides"    ],    "year": 2009,    "weight": 1.8,    "hardcover": true,    "publisher": {        "Company": "Pearson Education",        "Country": "India"    },    "website": null}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>网页的脚本代码就可以把此 JSON 文本解析为内部的数据结构去使用。</p><p>从此例子可看出，JSON 是树状结构，而 JSON 只包含 6 种数据类型：</p><ul><li>null: 表示为 null</li><li>boolean: 表示为 true 或 false</li><li>number: 一般的浮点数表示方式，在下一单元详细说明</li><li>string: 表示为 “…”</li><li>array: 表示为 [ … ]</li><li>object: 表示为 { … }</li></ul><p>我们要实现的 JSON 库，主要是完成 3 个需求：</p><ol><li>把 JSON 文本解析为一个树状数据结构（parse）。</li><li>提供接口访问该数据结构（access）。</li><li>把数据结构转换成 JSON 文本（stringify）。</li></ol><p><img src="images/requirement.png" alt="requirement"></p><p>我们会逐步实现这些需求。在本单元中，我们只实现最简单的 null 和 boolean 解析。</p><h2 id="2-搭建编译环境"><a href="#2-搭建编译环境" class="headerlink" title="2. 搭建编译环境"></a>2. 搭建编译环境</h2><p>我们要做的库是跨平台、跨编译器的，同学可使用任意平台进行练习。</p><p>练习源代码位于 <a href="https://github.com/miloyip/json-tutorial" target="_blank" rel="noopener">json-tutorial</a>，当中 tutorial01 为本单元的练习代码。建议同学登记为 GitHub 用户，把项目 fork 一个自己的版本，然后在上面进行修改。不了解版本管理的同学，也可以按右方「Clone or download」按钮，简单下载一个 zip 文件。</p><p>我们的 JSON 库名为 leptjson，代码文件只有 3 个：</p><ol><li><code>leptjson.h</code>：leptjson 的头文件（header file），含有对外的类型和 API 函数声明。</li><li><code>leptjson.c</code>：leptjson 的实现文件（implementation file），含有内部的类型声明和函数实现。此文件会编译成库。</li><li><code>test.c</code>：我们使用测试驱动开发（test driven development, TDD）。此文件包含测试程序，需要链接 leptjson 库。</li></ol><p>为了方便跨平台开发，我们会使用一个现时最流行的软件配置工具 <a href="https://cmake.org/" target="_blank" rel="noopener">CMake</a>。</p><p>在 Windows 下，下载安装 CMake 后，可以使用其 cmake-gui 程序：</p><p><img src="images/cmake-gui.png" alt="cmake-gui"></p><p>先在 “Where is the source code” 选择 json-tutorial/tutorial01，再在 “Where to build the binary” 键入上一个目录加上 /build。</p><p>按 Configure，选择编译器，然后按 Generate 便会生成 Visual Studio 的 .sln 和 .vcproj 等文件。注意这个 build 目录都是生成的文件，可以随时删除，也不用上传至仓库。</p><p>在 OS X 下，建议安装 <a href="https://brew.sh/" target="_blank" rel="noopener">Homebrew</a>，然后在命令行键入：</p><pre><code>$ brew install cmake$ cd github/json-tutorial/tutorial01$ mkdir build$ cd build$ cmake -DCMAKE_BUILD_TYPE=Debug ..$ make</code></pre><p>这样会使用 GNU make 来生成项目，把 Debug 改成 Release 就会生成 Release 配置的 makefile。</p><p>若你喜欢的话，CMake 也可以生成 Xcode 项目：</p><pre><code>$ cmake -G Xcode ..$ open leptjson_test.xcodeproj</code></pre><p>而在 Ubuntu 下，可使用 <code>apt-get</code> 来安装：</p><pre><code>$ apt-get install cmake</code></pre><p>无论使用什么平台及编译环境，编译运行后会出现：</p><pre><code>$ ./leptjson_test/Users/miloyip/github/json-tutorial/tutorial01/test.c:56: expect: 3 actual: 011/12 (91.67%) passed</code></pre><p>若看到类似以上的结果，说明已成功搭建编译环境，我们可以去看看那几个代码文件的内容了。</p><h2 id="3-头文件与-API-设计"><a href="#3-头文件与-API-设计" class="headerlink" title="3. 头文件与 API 设计"></a>3. 头文件与 API 设计</h2><p>C 语言有头文件的概念，需要使用 <code>#include</code>去引入头文件中的类型声明和函数声明。但由于头文件也可以 <code>#include</code> 其他头文件，为避免重复声明，通常会利用宏加入 include 防范（include guard）：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">#ifndef LEPTJSON_H__#define LEPTJSON_H__/* ... */#endif /* LEPTJSON_H__ */<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>宏的名字必须是唯一的，通常习惯以 <code>_H__</code> 作为后缀。由于 leptjson 只有一个头文件，可以简单命名为 <code>LEPTJSON_H__</code>。如果项目有多个文件或目录结构，可以用 <code>项目名称_目录_文件名称_H__</code> 这种命名方式。</p><p>如前所述，JSON 中有 6 种数据类型，如果把 true 和 false 当作两个类型就是 7 种，我们为此声明一个枚举类型（enumeration type）：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">typedef enum { LEPT_NULL, LEPT_FALSE, LEPT_TRUE, LEPT_NUMBER, LEPT_STRING, LEPT_ARRAY, LEPT_OBJECT } lept_type;<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>因为 C 语言没有 C++ 的命名空间（namespace）功能，一般会使用项目的简写作为标识符的前缀。通常枚举值用全大写（如 <code>LEPT_NULL</code>），而类型及函数则用小写（如 <code>lept_type</code>）。</p><p>接下来，我们声明 JSON 的数据结构。JSON 是一个树形结构，我们最终需要实现一个树的数据结构，每个节点使用 <code>lept_value</code> 结构体表示，我们会称它为一个 JSON 值（JSON value）。<br>在此单元中，我们只需要实现 null, true 和 false 的解析，因此该结构体只需要存储一个 lept_type。之后的单元会逐步加入其他数据。</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">typedef struct {    lept_type type;}lept_value;<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>C 语言的结构体是以 <code>struct X {}</code> 形式声明的，定义变量时也要写成 <code>struct X x;</code>。为方便使用，上面的代码使用了 <code>typedef</code>。</p><p>然后，我们现在只需要两个 API 函数，一个是解析 JSON：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">int lept_parse(lept_value* v, const char* json);<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>传入的 JSON 文本是一个 C 字符串（空结尾字符串／null-terminated string），由于我们不应该改动这个输入字符串，所以使用 <code>const char*</code> 类型。</p><p>另一注意点是，传入的根节点指针 v 是由使用方负责分配的，所以一般用法是：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">lept_value v;const char json[] = ...;int ret = lept_parse(&v, json);<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span></span></code></pre><p>返回值是以下这些枚举值，无错误会返回 <code>LEPT_PARSE_OK</code>，其他值在下节解释。</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">enum {    LEPT_PARSE_OK = 0,    LEPT_PARSE_EXPECT_VALUE,    LEPT_PARSE_INVALID_VALUE,    LEPT_PARSE_ROOT_NOT_SINGULAR};<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>现时我们只需要一个访问结果的函数，就是获取其类型：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">lept_type lept_get_type(const lept_value* v);<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><h2 id="4-JSON-语法子集"><a href="#4-JSON-语法子集" class="headerlink" title="4. JSON 语法子集"></a>4. JSON 语法子集</h2><p>下面是此单元的 JSON 语法子集，使用 <a href="https://tools.ietf.org/html/rfc7159" target="_blank" rel="noopener">RFC7159</a> 中的 <a href="https://tools.ietf.org/html/rfc5234" target="_blank" rel="noopener">ABNF</a> 表示：</p><pre><code>JSON-text = ws value wsws = *(%x20 / %x09 / %x0A / %x0D)value = null / false / true null  = "null"false = "false"true  = "true"</code></pre><p>当中 <code>%xhh</code> 表示以 16 进制表示的字符，<code>/</code> 是多选一，<code>*</code> 是零或多个，<code>()</code> 用于分组。</p><p>那么第一行的意思是，JSON 文本由 3 部分组成，首先是空白（whitespace），接着是一个值，最后是空白。</p><p>第二行告诉我们，所谓空白，是由零或多个空格符（space U+0020）、制表符（tab U+0009）、换行符（LF U+000A）、回车符（CR U+000D）所组成。</p><p>第三行是说，我们现时的值只可以是 null、false 或 true，它们分别有对应的字面值（literal）。</p><p>我们的解析器应能判断输入是否一个合法的 JSON。如果输入的 JSON 不合符这个语法，我们要产生对应的错误码，方便使用者追查问题。</p><p>在这个 JSON 语法子集下，我们定义 3 种错误码：</p><ul><li>若一个 JSON 只含有空白，传回 <code>LEPT_PARSE_EXPECT_VALUE</code>。</li><li>若一个值之后，在空白之后还有其他字符，传回 <code>LEPT_PARSE_ROOT_NOT_SINGULAR</code>。</li><li>若值不是那三种字面值，传回 <code>LEPT_PARSE_INVALID_VALUE</code>。</li></ul><h2 id="5-单元测试"><a href="#5-单元测试" class="headerlink" title="5. 单元测试"></a>5. 单元测试</h2><p>许多同学在做练习题时，都是以 <code>printf</code>／<code>cout</code> 打印结果，再用肉眼对比结果是否乎合预期。但当软件项目越来越复杂，这个做法会越来越低效。一般我们会采用自动的测试方式，例如单元测试（unit testing）。单元测试也能确保其他人修改代码后，原来的功能维持正确（这称为回归测试／regression testing）。</p><p>常用的单元测试框架有 xUnit 系列，如 C++ 的 <a href="https://github.com/google/googletest" target="_blank" rel="noopener">Google Test</a>、C# 的 <a href="https://www.nunit.org/" target="_blank" rel="noopener">NUnit</a>。我们为了简单起见，会编写一个极简单的单元测试方式。</p><p>一般来说，软件开发是以周期进行的。例如，加入一个功能，再写关于该功能的单元测试。但也有另一种软件开发方法论，称为测试驱动开发（test-driven development, TDD），它的主要循环步骤是：</p><ol><li>加入一个测试。</li><li>运行所有测试，新的测试应该会失败。</li><li>编写实现代码。</li><li>运行所有测试，若有测试失败回到3。</li><li>重构代码。</li><li>回到 1。</li></ol><p>TDD 是先写测试，再实现功能。好处是实现只会刚好满足测试，而不会写了一些不需要的代码，或是没有被测试的代码。</p><p>但无论我们是采用 TDD，或是先实现后测试，都应尽量加入足够覆盖率的单元测试。</p><p>回到 leptjson 项目，<code>test.c</code> 包含了一个极简的单元测试框架：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">#include <stdio.h>#include <stdlib.h>#include <string.h>#include "leptjson.h"static int main_ret = 0;static int test_count = 0;static int test_pass = 0;#define EXPECT_EQ_BASE(equality, expect, actual, format) \    do {\        test_count++;\        if (equality)\            test_pass++;\        else {\            fprintf(stderr, "%s:%d: expect: " format " actual: " format "\n", __FILE__, __LINE__, expect, actual);\            main_ret = 1;\        }\    } while(0)#define EXPECT_EQ_INT(expect, actual) EXPECT_EQ_BASE((expect) == (actual), expect, actual, "%d")static void test_parse_null() {    lept_value v;    v.type = LEPT_TRUE;    EXPECT_EQ_INT(LEPT_PARSE_OK, lept_parse(&v, "null"));    EXPECT_EQ_INT(LEPT_NULL, lept_get_type(&v));}/* ... */static void test_parse() {    test_parse_null();    /* ... */}int main() {    test_parse();    printf("%d/%d (%3.2f%%) passed\n", test_pass, test_count, test_pass * 100.0 / test_count);    return main_ret;}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>现时只提供了一个 <code>EXPECT_EQ_INT(expect, actual)</code> 的宏，每次使用这个宏时，如果 expect != actual（预期值不等于实际值），便会输出错误信息。<br>若按照 TDD 的步骤，我们先写一个测试，如上面的 <code>test_parse_null()</code>，而 <code>lept_parse()</code> 只返回 <code>LEPT_PARSE_OK</code>：</p><pre><code>/Users/miloyip/github/json-tutorial/tutorial01/test.c:27: expect: 0 actual: 11/2 (50.00%) passed</code></pre><p>第一个返回 <code>LEPT_PARSE_OK</code>，所以是通过的。第二个测试因为 <code>lept_parse()</code> 没有把 <code>v.type</code> 改成 <code>LEPT_NULL</code>，造成失败。我们再实现 <code>lept_parse()</code> 令到它能通过测试。</p><p>然而，完全按照 TDD 的步骤来开发，是会减慢开发进程。所以我个人会在这两种极端的工作方式取平衡。通常会在设计 API 后，先写部分测试代码，再写满足那些测试的实现。</p><h2 id="6-宏的编写技巧"><a href="#6-宏的编写技巧" class="headerlink" title="6. 宏的编写技巧"></a>6. 宏的编写技巧</h2><p>有些同学可能不了解 <code>EXPECT_EQ_BASE</code> 宏的编写技巧，简单说明一下。反斜线代表该行未结束，会串接下一行。而如果宏里有多过一个语句（statement），就需要用 <code>do { /*...*/ } while(0)</code> 包裹成单个语句，否则会有如下的问题：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">#define M() a(); b()if (cond)    M();else    c();/* 预处理后 */if (cond)    a(); b(); /* b(); 在 if 之外     */else          /* <- else 缺乏对应 if */    c();<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>只用 <code>{ }</code> 也不行：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">#define M() { a(); b(); }/* 预处理后 */if (cond)    { a(); b(); }; /* 最后的分号代表 if 语句结束 */else               /* else 缺乏对应 if */    c();<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>用 do while 就行了：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">#define M() do { a(); b(); } while(0)/* 预处理后 */if (cond)    do { a(); b(); } while(0);else    c();<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><h2 id="7-实现解析器"><a href="#7-实现解析器" class="headerlink" title="7. 实现解析器"></a>7. 实现解析器</h2><p>有了 API 的设计、单元测试，终于要实现解析器了。</p><p>首先为了减少解析函数之间传递多个参数，我们把这些数据都放进一个 <code>lept_context</code> 结构体：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">typedef struct {    const char* json;}lept_context;/* ... *//* 提示：这里应该是 JSON-text = ws value ws *//* 以下实现没处理最后的 ws 和 LEPT_PARSE_ROOT_NOT_SINGULAR */int lept_parse(lept_value* v, const char* json) {    lept_context c;    assert(v != NULL);    c.json = json;    v->type = LEPT_NULL;    lept_parse_whitespace(&c);    return lept_parse_value(&c, v);}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>暂时我们只储存 json 字符串当前位置，之后的单元我们需要加入更多内容。</p><p>若 <code>lept_parse()</code> 失败，会把 <code>v</code> 设为 <code>null</code> 类型，所以这里先把它设为 <code>null</code>，让 <code>lept_parse_value()</code> 写入解析出来的根值。</p><p>leptjson 是一个手写的递归下降解析器（recursive descent parser）。由于 JSON 语法特别简单，我们不需要写分词器（tokenizer），只需检测下一个字符，便可以知道它是哪种类型的值，然后调用相关的分析函数。对于完整的 JSON 语法，跳过空白后，只需检测当前字符：</p><ul><li>n ➔ null</li><li>t ➔ true</li><li>f ➔ false</li><li>“ ➔ string</li><li>0-9/- ➔ number</li><li>[ ➔ array</li><li>{ ➔ object</li></ul><p>所以，我们可以按照 JSON 语法一节的 EBNF 简单翻译成解析函数：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">#define EXPECT(c, ch) do { assert(*c->json == (ch)); c->json++; } while(0)/* ws = *(%x20 / %x09 / %x0A / %x0D) */static void lept_parse_whitespace(lept_context* c) {    const char *p = c->json;    while (*p == ' ' || *p == '\t' || *p == '\n' || *p == '\r')        p++;    c->json = p;}/* null  = "null" */static int lept_parse_null(lept_context* c, lept_value* v) {    EXPECT(c, 'n');    if (c->json[0] != 'u' || c->json[1] != 'l' || c->json[2] != 'l')        return LEPT_PARSE_INVALID_VALUE;    c->json += 3;    v->type = LEPT_NULL;    return LEPT_PARSE_OK;}/* value = null / false / true *//* 提示：下面代码没处理 false / true，将会是练习之一 */static int lept_parse_value(lept_context* c, lept_value* v) {    switch (*c->json) {        case 'n':  return lept_parse_null(c, v);        case '\0': return LEPT_PARSE_EXPECT_VALUE;        default:   return LEPT_PARSE_INVALID_VALUE;    }}<span aria-hidden="true" class="line-numbers-rows"><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span><span></span></span></code></pre><p>由于 <code>lept_parse_whitespace()</code> 是不会出现错误的，返回类型为 <code>void</code>。其它的解析函数会返回错误码，传递至顶层。</p><h2 id="8-关于断言"><a href="#8-关于断言" class="headerlink" title="8. 关于断言"></a>8. 关于断言</h2><p>断言（assertion）是 C 语言中常用的防御式编程方式，减少编程错误。最常用的是在函数开始的地方，检测所有参数。有时候也可以在调用函数后，检查上下文是否正确。</p><p>C 语言的标准库含有 <a href="https://en.cppreference.com/w/c/error/assert" target="_blank" rel="noopener"><code>assert()</code></a> 这个宏（需 <code>#include &lt;assert.h&gt;</code>），提供断言功能。当程序以 release 配置编译时（定义了 <code>NDEBUG</code> 宏），<code>assert()</code> 不会做检测；而当在 debug 配置时（没定义 <code>NDEBUG</code> 宏），则会在运行时检测 <code>assert(cond)</code> 中的条件是否为真（非 0），断言失败会直接令程序崩溃。</p><p>例如上面的 <code>lept_parse_null()</code> 开始时，当前字符应该是 <code>'n'</code>，所以我们使用一个宏 <code>EXPECT(c, ch)</code> 进行断言，并跳到下一字符。</p><p>初使用断言的同学，可能会错误地把含<a href="https://en.wikipedia.org/wiki/Side_effect_(computer_science" target="_blank" rel="noopener">副作用</a>)的代码放在 <code>assert()</code> 中：</p><pre class="line-numbers language-lang-c"><code class="language-lang-c">assert(x++ == 0); /* 这是错误的! */<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>这样会导致 debug 和 release 版的行为不一样。</p><p>另一个问题是，初学者可能会难于分辨何时使用断言，何时处理运行时错误（如返回错误值或在 C++ 中抛出异常）。简单的答案是，如果那个错误是由于程序员错误编码所造成的（例如传入不合法的参数），那么应用断言；如果那个错误是程序员无法避免，而是由运行时的环境所造成的，就要处理运行时错误（例如开启文件失败）。</p><h2 id="9-总结与练习"><a href="#9-总结与练习" class="headerlink" title="9. 总结与练习"></a>9. 总结与练习</h2><p>本文介绍了如何配置一个编程环境，单元测试的重要性，以至于一个 JSON 解析器的子集实现。如果你读到这里，还未动手，建议你快点试一下。以下是本单元的练习，很容易的，但我也会在稍后发出解答篇。</p><ol><li>修正关于 <code>LEPT_PARSE_ROOT_NOT_SINGULAR</code> 的单元测试，若 json 在一个值之后，空白之后还有其它字符，则要返回 <code>LEPT_PARSE_ROOT_NOT_SINGULAR</code>。</li><li>参考 <code>test_parse_null()</code>，加入 <code>test_parse_true()</code>、<code>test_parse_false()</code> 单元测试。</li><li>参考 <code>lept_parse_null()</code> 的实现和调用方，解析 true 和 false 值。</li></ol><h2 id="10-常见问答"><a href="#10-常见问答" class="headerlink" title="10. 常见问答"></a>10. 常见问答</h2><ol><li><p>为什么把例子命名为 leptjson？</p><p>来自于标准模型中的轻子（lepton），意为很轻量的 JSON 库。另外，建议大家为项目命名时，先 google 一下是否够独特，有很多同名的话搜寻时难以命中。</p></li><li><p>为什么使用宏而不用函数或内联函数？</p><p>因为这个测试框架使用了 <code>__LINE__</code> 这个编译器提供的宏，代表编译时该行的行号。如果用函数或内联函数，每次的行号便都会相同。另外，内联函数是 C99 的新增功能，本教程使用 C89。</p></li></ol><p>其他常见问答将会从评论中整理。</p><script>        document.querySelectorAll('.github-emoji')          .forEach(el => {            if (!el.dataset.src) { return; }            const img = document.createElement('img');            img.style = 'display:none !important;';            img.src = el.dataset.src;            img.addEventListener('error', () => {              img.remove();              el.style.color = 'inherit';              el.style.backgroundImage = 'none';              el.style.background = 'none';            });            img.addEventListener('load', () => {              img.remove();            });            document.body.appendChild(img);          });      </script>]]></content>
      
      
      <categories>
          
          <category> Markdown </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Typora </tag>
            
            <tag> Markdown </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2020/06/24/hello-world/"/>
      <url>/2020/06/24/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="noopener">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="noopener">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="noopener">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="noopener">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">$ hexo new "My New Post"<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="noopener">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">$ hexo server<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="noopener">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">$ hexo generate<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="noopener">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><pre class="line-numbers language-lang-bash"><code class="language-lang-bash">$ hexo deploy<span aria-hidden="true" class="line-numbers-rows"><span></span></span></code></pre><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html" target="_blank" rel="noopener">Deployment</a></p><script>        document.querySelectorAll('.github-emoji')          .forEach(el => {            if (!el.dataset.src) { return; }            const img = document.createElement('img');            img.style = 'display:none !important;';            img.src = el.dataset.src;            img.addEventListener('error', () => {              img.remove();              el.style.color = 'inherit';              el.style.backgroundImage = 'none';              el.style.background = 'none';            });            img.addEventListener('load', () => {              img.remove();            });            document.body.appendChild(img);          });      </script>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>third article</title>
      <link href="/2020/06/24/third-article/"/>
      <url>/2020/06/24/third-article/</url>
      
        <content type="html"><![CDATA[<script>        document.querySelectorAll('.github-emoji')          .forEach(el => {            if (!el.dataset.src) { return; }            const img = document.createElement('img');            img.style = 'display:none !important;';            img.src = el.dataset.src;            img.addEventListener('error', () => {              img.remove();              el.style.color = 'inherit';              el.style.backgroundImage = 'none';              el.style.background = 'none';            });            img.addEventListener('load', () => {              img.remove();            });            document.body.appendChild(img);          });      </script>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>second article</title>
      <link href="/2020/06/24/second-article/"/>
      <url>/2020/06/24/second-article/</url>
      
        <content type="html"><![CDATA[<script>        document.querySelectorAll('.github-emoji')          .forEach(el => {            if (!el.dataset.src) { return; }            const img = document.createElement('img');            img.style = 'display:none !important;';            img.src = el.dataset.src;            img.addEventListener('error', () => {              img.remove();              el.style.color = 'inherit';              el.style.backgroundImage = 'none';              el.style.background = 'none';            });            img.addEventListener('load', () => {              img.remove();            });            document.body.appendChild(img);          });      </script>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
